libplacebo provides the CMake target:

    find_package(libplacebo CONFIG REQUIRED)
    target_link_libraries(target PRIVATE libplacebo::libplacebo)

The initial Sunroom overlay port supports the Windows D3D11 backend only.

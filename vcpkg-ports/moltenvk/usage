MoltenVK provides the CMake target:

    find_package(MoltenVK CONFIG REQUIRED)
    target_link_libraries(main PRIVATE MoltenVK::MoltenVK)

The SunPlayer overlay uses Khronos' official static macOS archive.
